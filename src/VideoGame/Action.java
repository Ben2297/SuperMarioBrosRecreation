package VideoGame;

// Keeps track of which actions are being performed
public class Action {
    public int move; // -1 = move left, 0 = stationary, 1 = move right
    public boolean jump;
    public boolean enter;
}
